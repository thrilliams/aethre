// re-export because this is imported from a file rather than directly from a module,
// and keeping it contained would be nice
export type { Jsonify } from '@remix-run/server-runtime/dist/jsonify.d.ts';
